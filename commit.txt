# git 리파지토리 연결 및 소스 가져 오기
INSTRGRAM> git config --global user.name <user_name>
INSTRGRAM> git config --global user.email <user_email>
INSTRGRAM> git init
    Initialized empty Git repository in C:/Users/maxwe/Documents/pystudy/webStudy/MrBan_HTML_CSS/INSTRGRAM/.git/
INSTRGRAM> git remote add origin https://github.com/maxwell0212/mrBan_insta.git
INSTRGRAM> git pull origin master

# 작업 중이던 코드 푸시하기
git add .
git commit -m "ver 1.0 html 뼈대"
git push origin master


# 깃허브에 업로드된 코드를 다운로드 하는 방법
(기본형) git clone <리포지토리 주소>
(예시)   git clone https://github.com/maxwell0212/mrBan_insta.git

(기본형) git pull origin <브랜치 이름>
(예시)   git pull origin master